Create a comprehensive structured plan for the GenePattern module for 'STAR.aligner'.
            
            Tool Information:
            - Name: STAR.aligner
            - Version: Adding support for gzipped input data
            - Language: C++
            - Description: STAR (Spliced Transcripts Alignment to a Reference) 2.7.10b is a fast RNA-seq reads to genome mapper. It differs from other mappers as TopHat in that it gains speed at the expense of consuming more RAM and that it incorporates transcriptome annotation at the indexing build stage rather than at the analysis stage. It can optionally detect non-canonical splices and chimeric transcripts.
            
            Research Results:
            The following planning data has been gathered from existing module specifications:

{
  "module_name": "STAR.aligner",
  "parameters": [
    {
      "name": "star.index",
      "type": "FILE",
      "description": "A STAR index. Select a prebuilt index from https\\://datasets.genepattern.org/?prefix\\=data/module_support_files/STAR/ or upload your own as a ZIP file (you can make one using STAR.indexer).",
      "required": true,
      "default_value": "",
      "file_format": "zip",
      "num_values": "1..1"
    },
    {
      "name": "reads.pair.1",
      "type": "FILE",
      "description": "Unpaired reads or first mate for paired reads, as files in fastA or fastQ format. You can provide several files.",
      "required": true,
      "default_value": "",
      "file_format": "fa;fasta;fastq;fq",
      "num_values": "1+"
    },
    {
      "name": "reads.pair.2",
      "type": "FILE",
      "description": "Second mate for paired reads.<p><b><i>The files and the sequences inside the files must be in the same order as for the first mates.</i></b>",
      "required": false,
      "default_value": "",
      "file_format": "fa;fasta;fastq;fq",
      "prefix": "--reads2",
      "num_values": "0+"
    },
    {
      "name": "max.reads.to.align",
      "type": "Integer",
      "description": "Set this if you want to map only a selected set of reads at the top of the input (mainly useful for testing).",
      "required": false,
      "default_value": "",
      "prefix": "--mapN",
      "num_values": "0..1"
    },
    {
      "name": "align.read.end.to.end",
      "type": "TEXT",
      "description": "Align reads end to end and count all mistmatches. The default is to use instead \"soft clipping\" at both 5'- and 3'-end, that means to make a local alignment and ignore the contribution of the ends of the read to the final score if this improves the score. <b><i>This can be useful for ChIP-seq and other DNA sequencing applications with reads that have already been quality-trimmed.</i></b>",
      "required": true,
      "default_value": "no",
      "choices": "yes\\=yes;no\\=no",
      "num_values": "1..1"
    },
    {
      "name": "max.number.mismatches",
      "type": "Integer",
      "description": "Maximum number of mismatches per read. Note that a mate pair is counted as one read.",
      "required": true,
      "default_value": "10",
      "num_values": "1..1"
    },
    {
      "name": "max.fraction.mistaches",
      "type": "Floating Point",
      "description": "Maximum number of mismatches per read, expressed as the proportion between the number of mismatches and the <b><i>mapped</i></b> length of the read.  Note that a mate pair is counted as one read.",
      "required": true,
      "default_value": "0.3",
      "num_values": "1..1"
    },
    {
      "name": "min.overhang.annotated.read",
      "type": "Integer",
      "description": "Minimum length that read must map at both sides of splice junction in order to accept mapping to a splice junction that is annotated in GTF of tab file.",
      "required": true,
      "default_value": "3",
      "num_values": "1..1"
    },
    {
      "name": "min.overhang.not.annotated.read",
      "type": "Integer",
      "description": "Minimum length that read must map at both sides of splice junction in order to accept de novo discovery of splice junction.",
      "required": true,
      "default_value": "5",
      "num_values": "1..1"
    },
    {
      "name": "min.intron.length",
      "type": "Integer",
      "description": "Minimum size of intron. A gap in the alignment between a read and the genome that is smaller is considered a deletion, not an intron.",
      "required": true,
      "default_value": "21",
      "num_values": "1..1"
    },
    {
      "name": "max.intron.length",
      "type": "Integer",
      "description": "Maximum size of intron. If a read aligns to the genome with a gap larger than this it is considered a chimeric read. <b><i>The default value of 500,000 is fine-tuned to mammalian genomes, for plant and yeast genomes you will have to decrease it.</i></b>",
      "required": true,
      "default_value": "500000",
      "num_values": "1..1"
    },
    {
      "name": "mates.max.gap",
      "type": "Integer",
      "description": "Maximum distance between mate pair reads. If reads map to the genome farther apart the fragment is considered to be chimeric. <b><i>The default value of 500,000 is fine-tuned to mammalian genomes, for plant and yeast genomes you will have to decrease it.</i></b>",
      "required": true,
      "default_value": "500000",
      "num_values": "1..1"
    },
    {
      "name": "secondary.mapping.mismatches.range",
      "type": "Integer",
      "description": "By default STAR only reports reads that map to multiple locations on the genome when they map with the highest possible score. You can ask STAR to report also secondary mappings with up to this much more mismatches than the primary mappings.",
      "required": true,
      "default_value": "0",
      "num_values": "1..1"
    },
    {
      "name": "max.multimapping",
      "type": "Integer",
      "description": "Do not report reads that map to more than that many different locations on the genome.",
      "required": true,
      "default_value": "10",
      "num_values": "1..1"
    },
    {
      "name": "min.report.canonical.junction.overhang",
      "type": "Integer",
      "description": "Criterium for reporting de novo predicted splice junctions in the SJ.tab.out file \\: for canonical splice sites at least one read with at least this overhang is needed.",
      "required": true,
      "default_value": "12",
      "num_values": "1..1"
    },
    {
      "name": "min.report.noncanonical.junction.overhang",
      "type": "Integer",
      "description": "Criterium for reporting de novo predicted splice junctions in the SJ.tab.out file \\: for noncanonical splice sites at least 3 reads with at least this overhang are needed.",
      "required": true,
      "default_value": "30",
      "num_values": "1..1"
    },
    {
      "name": "map.only.reported.juntions",
      "type": "TEXT",
      "description": "By default all mapped reads are output in the SAM/BAM file. Set this to yes if you want to bring the SAM/BAM file in agreement with the SJ.tab.out file, by outputting only reads that are not spliced or map to splice junctions that are annotated or have been de novo predicted.",
      "required": true,
      "default_value": "no",
      "choices": "yes\\=yes;no\\=no",
      "num_values": "1..1"
    },
    {
      "name": "two.pass",
      "type": "TEXT",
      "description": "Run STAR in 2-pass mode, that is, run STAR a first time, merge the found splice junctions with the splice junction annotation in the index, and run STAR a second time. Consult the documentation for more explanation.",
      "required": true,
      "default_value": "no",
      "choices": "yes\\=yes;no\\=no",
      "num_values": "1..1"
    },
    {
      "name": "detect.chimeric.transcripts",
      "type": "TEXT",
      "description": "",
      "required": true,
      "default_value": "no",
      "choices": "yes\\=yes;no\\=no",
      "num_values": "1..1"
    },
    {
      "name": "output.unmapped.reads",
      "type": "TEXT",
      "description": "",
      "required": true,
      "default_value": "no",
      "choices": "yes\\=yes;no\\=no",
      "num_values": "1..1"
    },
    {
      "name": "quantify.genes",
      "type": "TEXT",
      "description": "Write a table with number of reads mapped per gene and a supplementary BAM file with mappings to transcriptome instead of to genome coordinates.",
      "required": true,
      "default_value": "no",
      "choices": "yes\\=yes;no\\=no",
      "num_values": "1..1"
    },
    {
      "name": "output.wiggle.file",
      "type": "TEXT",
      "description": "Output a \"wiggle\" file for viewing in viewers like IGV or UCSC genome browser.",
      "required": true,
      "default_value": "None",
      "choices": "None\\=none;bedGraph\\=file in BedGraph format;wiggle\\=file in WIG format",
      "num_values": "1..1"
    },
    {
      "name": "wiggle.signal",
      "type": "TEXT",
      "description": "From which bases to generate the signal for the \"wiggle\" file. By default they are all used, but you can choose to use only the bases at the 5'-end of the 1<sup>st</sup> read (useful for CAGE/RAMPAGE) or only from the 2<sup>nd</sup> mate from read pairs.",
      "required": true,
      "default_value": "all",
      "choices": "all\\=all;read1_5p\\=only 5' from read 1;read2\\=only from read 2",
      "num_values": "1..1"
    },
    {
      "name": "output.format",
      "type": "TEXT",
      "description": "",
      "required": true,
      "default_value": "SAM",
      "choices": "SAM\\=SAM unsorted;BAM Unsorted\\=BAM unsorted ;BAM SortedByCoordinate\\=BAM sorted by coordinate",
      "num_values": "1..1"
    },
    {
      "name": "HI.flag",
      "type": "TEXT",
      "description": "Number of \"hits\" with higest score to label as \"primary\" in the SAM/BAM file. If you choose \"only one\" the others are labeled as \"secondary\" (all reads that map with lower than the highest score are labeled \"secondary\").",
      "required": true,
      "default_value": "OneBestScore",
      "choices": "OneBestScore\\=only one;AllBestScore\\=all",
      "num_values": "1..1"
    },
    {
      "name": "output.prefix",
      "type": "TEXT",
      "description": "The prefix to use for the output file names.",
      "required": false,
      "default_value": "STAR",
      "prefix": "--outputprefix",
      "num_values": "0..1"
    }
  ],
  "docker_image_tag": "genepattern/star.aligner\\:v0.50",
  "categories": "RNA-seq",
  "task_type": "RNA-seq",
  "author": "Guy Bottu;V.I.B.-B.I.T.S.",
  "command_line": "perl /usr/local/bin/star_aligner_wrapper.pl --index <star.index> --reads1 <reads.pair.1> <reads.pair.2> <max.reads.to.align> --alignendtoend <align.read.end.to.end> --maxNmismatch <max.number.mismatches> --maxFmismatch <max.fraction.mistaches> --minoverhangannot <min.overhang.annotated.read> --minoverhangnotannot <min.overhang.not.annotated.read> --minintronlength <min.intron.length> --maxintronlength <max.intron.length> --matesmaxgap <mates.max.gap> --multimapmismatchrange <secondary.mapping.mismatches.range> --maxmultimap <max.multimapping> --canonicaloverhang <min.report.canonical.junction.overhang> --noncanonicaloverhang <min.report.noncanonical.junction.overhang> --maponlyreportedjunctions <map.only.reported.juntions> --twopass <two.pass> --detectchimeric <detect.chimeric.transcripts> --outputunmapped <output.unmapped.reads> --quantify <quantify.genes> --wiggle <output.wiggle.file> --wigglesignal <wiggle.signal>  --format <output.format> --HIflag <HI.flag> <output.prefix>"
}
            
            Please create a structured ModulePlan with:
            1. Detailed parameter definitions with types and descriptions
            2. Module architecture recommendations
            3. Integration strategy for GenePattern
            4. Validation and testing approach
            5. Implementation roadmap
            
            If an author name is not provided, use 'GenePattern Team'.
            
            Focus on creating actionable specifications for module development.